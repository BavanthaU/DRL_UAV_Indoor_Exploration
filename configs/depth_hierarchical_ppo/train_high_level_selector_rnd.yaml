run_name: train_high_level_selector_rnd
device: cpu
seed: 7
mode: freeze_local_train_high_level
baseline:
  task_id: Isaac-Depth-Hierarchical-PPO-UAV-Exploration-v0
  use_vlm: false
  use_convnext: false
  use_il: false
  use_sac: false
  curiosity_type: rnd
  local_use_new_cell_reward: false
  local_use_rnd: true
  high_level_use_map_rnd: true
  astar_selects_candidate: false
  learned_high_level_selector: true
  active_mapping_during_transit: true
  rnd_encoder: temporal_depth
  map_rnd_encoder: cnn_candidate
  wandb_log_replays: false
environment:
  backend: debug
  num_envs: 2
  max_steps: 64
  map_size: 32
ppo:
  max_iterations: 1
  num_envs: 2
  learning_rate: 3.0e-4
high_level:
  max_candidates: 64
  candidate_feature_dim: 24
  expected_candidate_gain: 500
map_rnd:
  feature_dim: 256
  rnd_lr: 1.0e-4
  reward_clip: 1.0
